apiVersion: v1
kind: Service
metadata:
  name: unityaspnetcoreapi
  labels:
    app: unityaspnetcoreapi
spec:
  type: LoadBalancer
  ports:
    # the port that this service should serve on
  - port: 80
  selector:
    app: unityaspnetcoreapi